// require a module that may not be installed.
function load(name) {
  try {
    return require(name);
  }
  catch(e) {
    if(e.code !== 'MODULE_NOT_FOUND') throw e;
  }
};

const r = require, t = load('prototoy');
